# Dotfiles secrets manifest
# Format: encrypted_filename:destination_path:permissions
#
# Add entries below, then run ./make-secrets.sh to build the
# age-encrypted bundle (secrets/secrets.tar.gz.age).
#
# Example:
# ssh_example.age:~/.ssh/example:600
# ssh_example.pub.age:~/.ssh/example.pub:644
